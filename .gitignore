node_modules/
dist/
.next/
out/
cache/
coverage/
__pycache__/
*.pyc
.pytest_cache/
# dependencies and build output — never committed (swarm-a was deleted for exactly this)
# local data and secrets
*.db
data/
.env.*
.env
broadcast/
.venv/

# Written by `next dev` (node_modules/next/dist/server/lib/generate-agent-files.js) and
# re-created when deleted. A dependency should not add project instructions to the repo.
web/AGENTS.md
web/CLAUDE.md

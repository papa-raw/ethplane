<!-- planning artifact: a day-one specification written for the local-model swarm on 2026-09-07/08. Historical; what shipped is described in docs/ and README.md, and differs where the record says so. -->


Day 1, swarm A, task 1 (contracts). In /home/ubuntu/swarm/ethplane create a Foundry project under contracts/ (forge init --no-git --force; solc 0.8.24; forge install OpenZeppelin/openzeppelin-contracts foundry-rs/forge-std) and implement exactly the spec in /home/ubuntu/swarm/spec/spec-contracts.md: src/PlaneToken.sol (ERC-20 "Plane"/"PLANE", 1_000_000e18 minted to the constructor's treasury, no further mint) and src/Ethplane.sol (the consolidated §3.3.2: Status PENDING/FAIL/PASS/REVIEW, Node struct with Split struct, registerLineage/acceptLineage, defineNode with strawmap check and split floors, setNodeVerifier, fundNode, recordBaseline once, claim from any known submission, heartbeat, forfeit, submit with ≤8 parents, recordMeasurement computing status, confirmReview by reviewer, _advance/_release cumulative pull-based credits, withdraw, closeNode, withdrawUnused pro-rata, EIP-712 relay claimFor/submitFor/registerLineageFor with nonce+deadline, seedStrawmap, setRelay). Tests in test/Ethplane.t.sol named exactly as the @test tags in /home/ubuntu/swarm/spec/gherkin-acceptance.feature for Feature blocks "Node definition and funding", "Leases", "Submission, measurement, verdict", "Money moves", "Recovery from an unverified frontier", "Lineage registration", "Review independence" (about 30 tests). Done when: `forge build` has no errors and `forge test` passes every test, and `git -C /home/ubuntu/swarm/ethplane status --short` shows only contracts/. Commit on branch swarm-a with message "contracts: PlaneToken + Ethplane per spec, N tests". Never push.
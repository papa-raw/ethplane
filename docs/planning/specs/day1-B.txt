<!-- planning artifact: a day-one specification written for the local-model swarm on 2026-09-07/08. Historical; what shipped is described in docs/ and README.md, and differs where the record says so. -->


Day 1, swarm B, task 1 (verifier runner, Python). In /home/ubuntu/swarm-b/ethplane create verifier/ per /home/ubuntu/swarm-b/spec/spec-verifier.md: run.py (arguments: artifact tarball path, reference leanVM path /home/ubuntu/swarm-b/leanVM pinned a210ef1b, baseline JSON) that: unpacks the diff, rejects any path outside crates/rec_aggregation/guests/aggregate.py and crates/lean_compiler/ (verdict false, reason frozen-path), applies it in a fresh git worktree, builds with cargo build --release, hashes the binary and refuses to measure if the diff is non-empty but the hash equals the reference's (reason stale-binary), runs the pinned command `cargo run --release -- aggregate --xmss 900 --log-inv-rate 1 --repeat 3` three times, parses cycles / proving time / proof size / verifying with the regexes in the spec, and writes verdict JSON {cycles, provingMicros, proofSizeBytes, verifyMicros, verifierAccepted, reason, binarySha256, status} where status follows the spec (FAIL / PASS / REVIEW at >20 % drop). parse.py with the four regexes and unit tests (tests/test_parse.py, pytest, using sample output text you capture from one real run of the pinned command in the worktree). Done when: `python3.12 -m pytest verifier/tests` passes, and `python3.12 verifier/run.py --self-test` runs the pinned command once and prints a verdict JSON with cycles 1542812. Commit on branch swarm-b with message "verifier: run.py + parse.py + tests". Never push. Read spec-verifier.md fully before writing; the python verifier of leanVM itself (python-verifier/verifier.py) is called as a subprocess, not reimplemented.